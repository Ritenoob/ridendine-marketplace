# Dependencies
node_modules/
.pnpm-store/
.local-tools/

# Build outputs
.next/
dist/
build/
out/

# Environment - NEVER commit secrets
.env
.env.local
.env.*.local
.env.vercel
.env.production
.env.*
!.env.example

# Private keys (never commit)
*.pem
*.key

# Partner integration — local Vercel env handoff files carry the partner secret
*-vercel.env
docs/partner-integration/**/partner-vercel.env

# Legacy test credentials file (removed in rebuild Phase 1).
# Listed here so a future accidental paste of the same filename never re-enters the repo.
TEST_CREDENTIALS.md

# IDE
.vscode/
.idea/
*.swp
*.swo

# Obsidian (root-level only — docs/.obsidian is tracked)
/.obsidian/
Untitled.canvas

# OS
.DS_Store
Thumbs.db

# Turbo
.turbo/

# Debug
npm-debug.log*
pnpm-debug.log*
yarn-debug.log*
yarn-error.log*

# Testing
coverage/
.nyc_output/
playwright-report/
test-results/

# Supabase
supabase/.branches/
supabase/.temp/

# Misc
*.log
*.tsbuildinfo

.vercel
.env*.local

# Graphify knowledge-graph output (generated; appears in several directories)
**/graphify-out/

.claude/settings.local.json

# Claude Flow agent session data (runtime artifacts, not source)
.claude-flow/
